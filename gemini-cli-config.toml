# gemini-cli-config.toml

# configuration for Global Installation
# usage: 
# 1. Copy the 'tsumiki_gemini' folder to '~/.gemini/tsumiki'
# 2. Add the following configuration to your '~/.gemini/config.toml'

[commands]
include = ["~/.gemini/tsumiki/commands"]